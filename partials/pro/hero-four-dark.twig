{% set articleCount = 0 %}



<section class="row">
    <div class="col-sm-12 section-header u-mobile-no-padding">
        <a href="{{link}}">       
            <h2 class="pro-main__header">{{ title }}</h2>
        </a>
    </div>

    <div class="col-sm-12">
        {% if articles[articleCount] %}
            {{this.render('../_single-article.twig', {
                article: articles[articleCount], 
                imageSize: {width:414, height:255},
                site: site,
                containerClass: 'card-rec card-rec-tablet card-sm-mobile inverted u-margin-bottom-30 u-mobile-no-margin-bottom u-mobile-no-border-bottom'
            }) | raw}}
            {% set articleCount = (dev) ? articleCount : articleCount + 1 %}
        {% endif %}


    </div>
    {% set mobBorders = [' u-mobile-no-border-bottom',' u-mobile-no-border-bottom',''] %}    
    {% for i in 0..2 if articles[articleCount] %}
        <div class="col-md-4 col-sm-4 col-xs-12">
        {{this.render('../_single-article.twig', {
            article: articles[articleCount], 
            imageSize: {width:300, height:160},
            site: site,
            containerClass: 'card-sm card-sm-tablet card-sm-mobile u-no-margin-bottom'~mobBorders[i]
        }) | raw}}
        {% set articleCount = (dev) ? articleCount : articleCount + 1 %}
        
        </div>
    {% endfor %}

    {% set limit = 4 %}

    {% set blogguid = articles[0].blog['guid'] %}

    {# <div class="col-12">{{_AppHelper.printArray(blogId)}}</div> #}
    
    {% set limit = 4 %}
    {# <div class="col-sm-12">
        <div class="row">
            {% if articles|length >= limit %}
                {{ this.render('../ajaxarticles_new.twig', {
                    name            : "section-load",
                    limit           : limit,
                    offset          : limit,
                    blogguid        : blogguid,
                    label           : "Load more",
                    imgwidth        : "300",
                    imgheight       : "160",
                    container       : "mainAjaxArticles",
                    non_pinned      : 0, 
                    card_class      : "col-sm-3 card-sm card-sm-tablet card-sm-mobile",
                    buttonClass     : "loadMoreArticles"
                }) | raw }}
            {% endif %}
        </div>
    </div> #}
</section>