
    <!-- presspatron tag - no local change -->
    {% if 'article' not in pageClass %}
        {% if (blogName | replace({" ": "_"}) | lower) == 'lockerroom' %}
            {% set ppsrc = '//dashboard.presspatron.com/dev/banner?b=9Ac35CVe5uEb9P8vFFb5YjFu' %}
        {% else %}
            {% set ppsrc = '//dashboard.presspatron.com/dev/banner?b=AbkE2e7ZwQgwLPFzVowC2cKm' %}
        {% endif %}

            <script>
                (function(d) {
                var el, fjs = d.getElementsByTagName('script')[0];
                    el = d.createElement('script');
                    el.id = "js-pp-banner";
                    el.title ="Press Patron";
                    el.src = "{{ppsrc}}";
                    fjs.parentNode.insertBefore(el, fjs);
                }(document));
            </script>
    {% endif %}
    <!-- end presspatron tag -->

    <!-- Google Tag Manager - edit in integrations -->
    {% if networkData.thirdPartyIntegrations['googleTagManager'] | length > 0 %}
        {{networkData.thirdPartyIntegrations['googleTagManager']['script'] | raw}}
    {% endif %}
    <!-- Google Tag Manager - END -->


    {# google analytics - no local change #}
    {% set googleAnalyitcsId = ''%}
    {% if networkData.thirdPartyIntegrations['googleAnalytics']['id'] is defined and  networkData.thirdPartyIntegrations['googleAnalytics']['id'] is not empty  %}
        {% set googleAnalyitcsId = networkData.thirdPartyIntegrations['googleAnalytics']['id'] %}
    {% endif %}

    {% if googleAnalyitcsId is defined and googleAnalyitcsId is not empty%}
        <!-- Google Analytics - no local change apart from ID -->
        <script>
        (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
        (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
        m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
        })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

        ga('create', '{{googleAnalyitcsId}}', 'auto');
        ga('send', 'pageview');
        </script>
        <!-- End Google Analytics -->
    {% endif %}

     {# google analytics tag - end #}
    

    
    




    {# adsense code????? think this is superfluous #}

    {# <script async src="//pagead2.googlesyndication.com/pagead/js/adsbygoogle.js"></script>

    <script>
      (adsbygoogle = window.adsbygoogle || []).push({
        google_ad_client: "ca-pub-9270963482105934",
        enable_page_level_ads: true
      });
    </script> #}
    

    <!-- profitwell code - no local edit -->
    {% set useremail = '' %}
    {% if not _User.isGuest() %}
        {% set user = _User.getUser() %}
        {% set useremail = "'user_email': '"~user.email~"'" %}
    {% endif %}
    <script id="profitwell-js" data-pw-auth="39e1c7788f6a7b23602afc0b12646e50"> 
        (function(i,s,o,g,r,a,m){
            i[o]=i[o]||function(){(i[o].q=i[o].q||[]).push(arguments)}; 
            a=s.createElement(g);
            m=s.getElementsByTagName(g)[0];
            a.async=1;
            a.src=r+'?auth='+ s.getElementById(o+'-js').getAttribute('data-pw-auth');
            m.parentNode.insertBefore(a,m);
        })
         (window,document,'profitwell','script','https://public.profitwell.com/js/profitwell.js');
         profitwell('start', { {{useremail|raw}} }); 
    </script>
    <!-- profitwell code end -->



{# <!-- Facebook Pixel Code -->
<script>
  !function(f,b,e,v,n,t,s)
  {if(f.fbq)return;n=f.fbq=function(){n.callMethod?
  n.callMethod.apply(n,arguments):n.queue.push(arguments)};
  if(!f._fbq)f._fbq=n;n.push=n;n.loaded=!0;n.version='2.0';
  n.queue=[];t=b.createElement(e);t.async=!0;
  t.src=v;s=b.getElementsByTagName(e)[0];
  s.parentNode.insertBefore(t,s)}(window,document,'script',
  'https://connect.facebook.net/en_US/fbevents.js');
  fbq('init', '193452251391850'); 
  fbq('track', 'PageView');
</script>
<noscript>
  <img height="1" width="1" 
  src="https://www.facebook.com/tr?id=193452251391850&ev=PageView
  &noscript=1"/>
</noscript>
<!-- End Facebook Pixel Code --> #}




{# OLD AD CODE BELOW -- USE FOR REF ONLY #}

{# <script async='async' src='https://www.googletagservices.com/tag/js/gpt.js'></script>
<script>
  var googletag = googletag || {};
  googletag.cmd = googletag.cmd || [];
</script>

 
{% set blogNetworkName = (blogData.title | lower) %}
{% if blogNetworkName == 'ideasroom' %}
    {% set adPlace = 'futurelearning' %}
{% else %}
    {% set adPlace = 'newsroom' %}
{% endif %}

{% set dfpAcct = '268436641' %}




<script>

  var setTags = {

    "getpro": "https://8621311.fls.doubleclick.net/activityi;src=8621311;type=getpro;cat=getpr0;dc_lat=;dc_rdid=;tag_for_child_directed_treatment=;ord=",
    "learnmore": "https://8621311.fls.doubleclick.net/activityi;src=8621311;type=learnmor;cat=learn0;dc_lat=;dc_rdid=;tag_for_child_directed_treatment=;ord=",
    "signup": "https://8621311.fls.doubleclick.net/activityi;src=8621311;type=signup;cat=signu0;dc_lat=;dc_rdid=;tag_for_child_directed_treatment=;ord="

  }

  var screenWidth = screen.width;

  googletag.cmd.push(function() {         
                googletag.pubads().enableSingleRequest();
                googletag.pubads().collapseEmptyDivs();
                googletag.pubads().setCentering(true);
                googletag.enableServices();
                
            });
    
  var nextBannerId = 1;
  function generateNextBannerName() {
     var id = nextBannerId++;
     return 'adBanner' + id;
  }

  var nextMrecId = 1;
  function generateNextMrecName() {
     var id = nextMrecId++;
     return 'adMrec' + id;
  }

  var nextHalfId = 1;
  function generateNextHalfName() {
     var id = nextHalfId++;
     return 'adHalf' + id;
  }

  function loadNextBannerAd(blogAd) {
    var altAds = document.getElementById("specifyads");
 
    if (altAds) {
      var blogName = altAds.className;
    } else {
      var blogName = '{{adPlace}}';
    }
    if (blogAd != 'futurelearning') {blogAd = blogName}
     var adSlot = document.getElementById("newAdBanner");
     var slotName = generateNextBannerName();
     var slotDiv = document.createElement('div');
     adSlot.appendChild(slotDiv);
     slotDiv.id = slotName;
     slotDiv.setAttribute( 'class', 'google_ad' );
     adSlot.id = "";
     adSlot.setAttribute( 'class', 'bannerad d-print-none' );
     
     googletag.cmd.push(function() {
       var slot = googletag.defineSlot('/{{dfpAcct}}/'+blogAd, [[970, 250], [728, 250], [728, 90], [300, 75], [300,100]], slotName).setTargeting().defineSizeMapping([[[1025,400],[970,250]],[[767,400],[[728,250],[728,90]]],[[0,250],[[300,75],[300,100]]]]).addService(googletag.pubads());
       googletag.display(slotName);
     });
  }

  function loadNextMrecAd(blogAd,maxSize) {

    if (screenWidth >= maxSize) { 
      console.log('mrec too big',screenWidth);
      return;
    };
    var altAds = document.getElementById("specifyads");
 
    if (altAds) {
      var blogName = altAds.className;
    } else {
      var blogName = '{{adPlace}}';
    }
     if (blogAd != 'futurelearning') {blogAd = blogName}
     var adSlot = document.getElementById("newAdMrec");
     var slotName = generateNextMrecName();
     var slotDiv = document.createElement('div');
     adSlot.appendChild(slotDiv);
     slotDiv.id = slotName;
     slotDiv.setAttribute( 'class', 'google_ad' );
     adSlot.id = "";
     adSlot.setAttribute( 'class', 'boxad d-print-none' );
     
     googletag.cmd.push(function() {
       var slot = googletag.defineSlot('/{{dfpAcct}}/'+blogAd, [[300,250],[300, 75],[300,100]], slotName).setTargeting().defineSizeMapping([[[767,0],[300,250]],[[0,0],[[300,75],[300,100]]]]).addService(googletag.pubads());
       googletag.display(slotName);
     });
  }

  function loadNextHalfAd(blogAd,minSize) {
    if (screenWidth <= minSize) { 
      console.log('hp too small',screenWidth);
      return;
       }
    var altAds = document.getElementById("specifyads");
 
      if (altAds) {
        var blogName = altAds.className;
      } else {
        var blogName = '{{adPlace}}';
      }
     if (blogAd != 'futurelearning') {blogAd = blogName}
     var adSlot = document.getElementById("newAdHalf");
     var slotName = generateNextHalfName();
     var slotDiv = document.createElement('div');
     adSlot.appendChild(slotDiv);
     slotDiv.id = slotName;
     slotDiv.setAttribute( 'class', 'google_ad' );
     adSlot.id = "";
     adSlot.setAttribute( 'class', 'sidepanelAd d-print-none' );
     
     googletag.cmd.push(function() {
       var slot = googletag.defineSlot('/{{dfpAcct}}/'+blogAd, [[300,600],[728,250],[728,90],[300,250]], slotName).setTargeting().defineSizeMapping([[[992,0],[300,600]],[[767,0],[[728,250],[728,90]]],[[0,0],[300,250]]]).addService(googletag.pubads());
       googletag.display(slotName);
     });
  }

  function loadTwoAds(blogAd) {

     var altAds = document.getElementById("specifyads");
      if (altAds) {
        var blogName = altAds.className;
      } else {
        var blogName = '{{adPlace}}';
      }
     if (blogAd != 'futurelearning' || blogName == 'none') {blogAd = blogName}
     var adSlot = document.getElementById("newAdBanner");
     var slotName = generateNextBannerName();
     var slotDiv = document.createElement('div');
     adSlot.appendChild(slotDiv);
     slotDiv.id = slotName;
     slotDiv.setAttribute( 'class', 'google_ad' );
     adSlot.id = "";
     adSlot.setAttribute( 'class', 'bannerad d-print-none' );
     var adSlot2 = document.getElementById("newAdHalf");
     var slotName2 = generateNextHalfName();
     var slotDiv2 = document.createElement('div');
     adSlot2.appendChild(slotDiv2);
     slotDiv2.id = slotName2;
     slotDiv2.setAttribute( 'class', 'google_ad' );
     adSlot2.id = "";
     adSlot2.setAttribute( 'class', 'sidepanelAd d-print-none' );
     
     googletag.cmd.push(function() {
       var slot = googletag.defineSlot('/{{dfpAcct}}/'+blogAd, [[970, 250], [728, 250], [728, 90], [300, 75],[300,100]], slotName).setTargeting().defineSizeMapping([[[1025,400],[970,250]],[[767,400],[[728,250],[728,90]]],[[0,250],[[300,75],[300,100]]]]).addService(googletag.pubads());
       var slot2 = googletag.defineSlot('/{{dfpAcct}}/'+blogAd, [[300,600],[728,250],[728,90],[300,250]], slotName2).setTargeting().defineSizeMapping([[[992,0],[300,600]],[[767,0],[[728,250],[728,90]]],[[0,0],[300,250]]]).addService(googletag.pubads());
       googletag.display(slotName);
     });
  }

  function loadCanonAds(){
    var adSlot = document.getElementById("canon-mrec");
     var slotName = generateNextMrecName();
     var slotDiv = document.createElement('div');
     adSlot.appendChild(slotDiv);
     slotDiv.id = slotName;
     slotDiv.setAttribute( 'class', 'google_ad' );
     adSlot.id = "";
     adSlot.setAttribute( 'class', 'd-print-none' );
     //var adSlot2 = document.getElementById("canon-banner");
     //var slotName2 = generateNextBannerName();
     //var slotDiv2 = document.createElement('div');
     //adSlot2.appendChild(slotDiv2);
     //slotDiv2.id = slotName2;
     //slotDiv2.setAttribute( 'class', 'google_ad' );
     //adSlot2.id = "";
     //adSlot2.setAttribute( 'class', 'd-print-none' );
     //console.log(adSlot2);
     
     googletag.cmd.push(function() {
       var slot = googletag.defineSlot('/{{dfpAcct}}/canon', [[300, 250]], slotName).setTargeting().defineSizeMapping([[[0,250],[300,250]]]).addService(googletag.pubads());
       //var slot2 = googletag.defineSlot('/{{dfpAcct}}/canon', [[970,339]], slotName2).setTargeting().defineSizeMapping([[[992,0],[970,339]],[[0,250],[]]]).addService(googletag.pubads());
       googletag.display(slotName);
     });
  } 

  

</script> #}